<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpsCore.UI.Views.EmployeesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:OpsCore.Core.Models"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Employees" FontSize="28" FontWeight="Bold" Margin="0,0,0,16"/>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" PlaceholderText="Search employees..." Width="300" HorizontalAlignment="Left" Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Column="1" Content="+ Add Employee" Style="{StaticResource AccentButtonStyle}" Click="AddEmployee_Click"/>
        </Grid>

        <Grid Grid.Row="2" Padding="12,8" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Name" FontWeight="Bold" Opacity="0.7"/>
            <TextBlock Grid.Column="1" Text="Department" FontWeight="Bold" Opacity="0.7"/>
            <TextBlock Grid.Column="2" Text="Present" FontWeight="Bold" Opacity="0.7"/>
            <TextBlock Grid.Column="3" Text="" Width="80"/>
        </Grid>

        <ListView Grid.Row="3" ItemsSource="{x:Bind ViewModel.Employees, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Employee">
                    <Grid Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind FirstName}"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind Department.Name}"/>
                        <ToggleButton Grid.Column="2"
                                      IsChecked="{x:Bind IsPresent, Mode=OneWay}"
                                      Click="TogglePresent_Click"
                                      Content="Present"/>
                        <Button Grid.Column="3" Content="Delete" Click="DeleteEmployee_Click" Tag="{x:Bind}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>